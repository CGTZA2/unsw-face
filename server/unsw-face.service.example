[Unit]
Description=UNSW Face Test API
After=network.target

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/unsw-face
EnvironmentFile=/opt/unsw-face/.env
ExecStart=/usr/bin/node --env-file=.env api/server.js
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
